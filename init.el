(doom! :checkers
       (syntax +childframe)
       (spell +everywhere)
       :completion
       (company +childframe)
       (vertico +icons +childframe)
       :config
       (default +bindings +smartparens)
       :editor
       (format +onsave)
       snippets
       :emacs
       (ibuffer +icons)
       (dired)
       :lang
       emacs-lisp
       json
       markdown
       (python +pyenv)
       (java)
       kotlin
       org
       :tools
       (magit)
       (editorconfig)
       :ui
       doom
       doom-dashboard
       workspaces
       modeline
       treemacs
       )
